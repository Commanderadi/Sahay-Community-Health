# Copy to .env for local overrides. Not required for local dev
# (defaults to http://localhost:5000).

# URL of the backend API. Set this in the Netlify site settings for production,
# e.g. https://sahay-backend.onrender.com
REACT_APP_API_URL=
